# PROTOTYPE — an EXPLICIT NULL (premise 18). Absent means nobody looked; null means it
# was ruled out. The catch-all (#230) depends on being able to tell them apart, so a null
# is a cell FILE, exactly like a ruled-in one, and never a missing file.

key:
  intent: add
  class: curriculum-open

ruling: null

null_ruling:
  reason: >
    curriculum/open.md is `origin: generated`. Its rows are a function of tracker state;
    there is no act of adding one. A hand-added row is not a wrong row, it is a row that
    the next regeneration silently deletes — the failure mode is invisible, which is why
    this is ruled rather than left absent.
  redirect: regenerate@curriculum-open
  ruled_by: "premise 4 — the generated flag turns 'do not hand-edit this' from a convention into a property"
  header_says_so: "curriculum/open.md line 1: GENERATED by tools/snapshot_debt.py — do not edit."
